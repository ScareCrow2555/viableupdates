# News application update channel

Reserved for a future Viable Food news application release manifest and encrypted update assets.

When activated, this directory should follow the same pattern as `apps/job-board/`: a stable `latest.json` plus immutable version manifests under `releases/`.
